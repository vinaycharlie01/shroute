# .claude/output-styles/

Project-scoped output styles (custom system-prompt presets selected via the
`outputStyle` setting). Most output styles are personal and live in
`~/.claude/output-styles/` instead — put one here only if the whole team
should share it (e.g. a stricter "review mode" everyone uses on this repo).

Empty for now; nothing team-wide has been needed yet.
