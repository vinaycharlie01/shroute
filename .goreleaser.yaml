version: 2

project_name: sh-mcp-go

before:
  hooks:
    - go mod tidy

builds:
  - id: sh-mcp-go
    main: ./cmd/sh-mcp-go
    binary: sh-mcp-go
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X github.com/vinaycharlie01/sh-mcp-go/pkg/version.Version={{.Version}}
      - -X github.com/vinaycharlie01/sh-mcp-go/pkg/version.Commit={{.Commit}}
      - -X github.com/vinaycharlie01/sh-mcp-go/pkg/version.BuildDate={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude: ["^docs:", "^test:", "^ci:", "Merge pull request", "Merge branch"]

release:
  github:
    owner: vinaycharlie01
    name: sh-mcp-go
