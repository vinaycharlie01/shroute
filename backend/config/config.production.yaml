env: production

log:
  level: info
  format: json

# Secrets (mongo.uri, redis.password, etc) must never be committed here —
# set them via environment variables (APP_MONGO_URI, APP_REDIS_PASSWORD,
# ...) or a .env file excluded from version control. See backend/.env.example.
